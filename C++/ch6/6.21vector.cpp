#include <iostream>
#include <vector>

using namespace std;

int main()
{
    //std::array<int, 5> array; array를 정의할때는 이렇게 사이즈를 반드시 적어주었어야 함
    // std::vector<int> array;

    // vector<int> array2 = {1, 2, 3, 4, 5};

    // cout << array2.size() << endl;

    // std::vector<int> array3 = {1, 2, 3};

    // cout << array3.size() << endl;

    // std::vector<int> array4{1, 2, 3};

    // cout << array4.size() << endl;
    // return 0;
}